% OVERRIDE of a tier-1 shared file. main's version lists all thirteen courses;
% this variant carries eight because the full list does not fit the one-page
% budget, and the heading is retitled (see below).
%
% Rebase hazard: because these \item lines are a subset of main's, in main's
% order, git will often AUTO-MERGE a course added on main into this list
% rather than raising a conflict -- silently, with no overfull warning. The
% page budget is ~10pt, roughly one two-column row, so two absorbed courses
% push this CV onto a second page. The CI page-count check guards against it;
% if it fails after a rebase, a shared course has been merged in and should be
% removed here rather than kept.
%
% The heading says SELECTED COURSES TAKEN, not SELECTED COURSES, because this
% section sits below TUTORING EXPERIENCE, where "courses" alone invites
% reading the list as courses tutored rather than courses taken -- the
% opposite of what it evidences. The retitle is deliberately local to this
% variant; every other branch keeps the shared SELECTED COURSES heading,
% having no tutoring section to be confused with.
\newcommand{\sectionCourses}{%
\section*{SELECTED COURSES TAKEN}%
\begin{cvcolumns}{2}
\begin{itemize}
    \item Astrostatistics (Cambridge, 2025)
    \item Cosmology (Cambridge, 2024)
    \item General Relativity (Cambridge, 2024)
    \item Computational Physics (Toronto, 2023)
    \item Computational Astrophysics (Toronto, 2023)
    \item Advanced Classical Mechanics (Toronto, 2023)
    \item Geometry of Curves and Surfaces (Toronto, 2023)
    \item Partial Differential Equations (Toronto, 2023)
\end{itemize}
\end{cvcolumns}
}
