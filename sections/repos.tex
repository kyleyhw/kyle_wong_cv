% Variant-specific section. Defined here as an empty stub on main.
% Each variant branch overrides this file with its own
% \newcommand{\sectionRepos}{...} containing variant-specific content.
\newcommand{\sectionRepos}{}
