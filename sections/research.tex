% Variant-specific section. Defined here as an empty stub on main.
% Each variant branch (academic, industry, industry-<role>, academic-<role>) overrides this file
% with its own \newcommand{\sectionResearch}{...} containing variant-specific content.
\newcommand{\sectionResearch}{}
